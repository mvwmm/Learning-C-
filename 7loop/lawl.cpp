// Gng what you need remember is that you should always write the checking statement inside round bracket.
// Example: while (i = 5)
// 2nd example: while (i >= 5)
// Once you know this this XP Level becomes really easy to obtain ngl

#include <iostream>

int main() {
    int i = 0;
    while (i <= 5)
    {
        std::cout << i << "\n";
        i += 1;
    }
}